{
  "name": "avilease-dw-demo",
  "version": "1.0.0",
  "private": true,
  "description": "AviLease enterprise data warehouse demo — delivered by Prodigy",
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "seed": "python scripts/seed_demo_data.py",
    "install:all": "npm install && npm --prefix client install"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mssql": "^10.0.4"
  }
}
